- game = this

.team
  - each @duders, (duder) ->
    = game.render "duder", duder

.opponents
  - each @opponents, (duder) ->
    = game.render "duder", duder

%pre.events
  - each @events, (event) ->
    = event
